<IfModule mod_rewrite.c>
    RewriteEngine On

    # If request is not already for public folder
    RewriteCond %{REQUEST_URI} !^/public/

    # Rewrite all requests to public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
